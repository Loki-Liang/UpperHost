<UserControl x:Class="UpperHost.Presentation.Wpf.Controls.AlarmPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:UpperHost.Presentation.Wpf.Controls">
    <DataGrid ItemsSource="{Binding Alarms, RelativeSource={RelativeSource AncestorType={x:Type local:AlarmPanel}}}"
              AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="100" />
            <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="130" />
            <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="120" />
            <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="3*" />
            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120" />
            <DataGridTextColumn Header="Raised" Binding="{Binding RaisedAt}" Width="180" />
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
