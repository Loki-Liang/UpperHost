<UserControl x:Class="UpperHost.Presentation.Wpf.Controls.DeviceList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:UpperHost.Presentation.Wpf.Controls">
    <ListView ItemsSource="{Binding Devices, RelativeSource={RelativeSource AncestorType={x:Type local:DeviceList}}}">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Device" DisplayMemberBinding="{Binding Descriptor.DisplayName}" Width="220" />
                <GridViewColumn Header="State" DisplayMemberBinding="{Binding State}" Width="100" />
                <GridViewColumn Header="Model" DisplayMemberBinding="{Binding Descriptor.Model}" Width="160" />
                <GridViewColumn Header="Serial" DisplayMemberBinding="{Binding Descriptor.SerialNumber}" Width="160" />
            </GridView>
        </ListView.View>
    </ListView>
</UserControl>
