<UserControl x:Class="UpperHost.Presentation.Wpf.Controls.CommandPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:UpperHost.Presentation.Wpf.Controls">
    <ItemsControl ItemsSource="{Binding Commands, RelativeSource={RelativeSource AncestorType={x:Type local:CommandPanel}}}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Button Content="{Binding DisplayName}" ToolTip="{Binding Description}" Tag="{Binding}"
                        Padding="12,6" Margin="4" Click="Command_Click" />
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
