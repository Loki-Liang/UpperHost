<Window x:Class="UpperHost.Sample.DeviceControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="UpperHost Device Control Sample"
        Width="720" Height="470"
        MinWidth="620" MinHeight="420">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock FontSize="28" FontWeight="SemiBold" Text="Device Control Sample" />
            <TextBlock Margin="0,6,0,0" Text="Typed commands → protocol → simulator transport → readback" />
        </StackPanel>

        <Border Grid.Row="1" Margin="0,22,0,0" Padding="14" BorderBrush="Gray" BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Text="Device state" />
                <TextBlock x:Name="DeviceStateText" Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center" Text="Offline" />
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Width="92" Margin="0,0,8,0" Click="Connect_Click" Content="Connect" />
                    <Button Width="92" Click="Disconnect_Click" Content="Disconnect" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="Target °C" />
            <TextBox x:Name="TargetTextBox" Grid.Column="1" Margin="10,0,14,0" Padding="6" Text="30" />
            <Button Grid.Column="2" Width="100" Margin="0,0,8,0" Click="SetTarget_Click" Content="Set + readback" />
            <Button Grid.Column="3" Width="100" Click="ReadStatus_Click" Content="Read status" />
        </Grid>

        <StackPanel Grid.Row="3" Margin="0,16,0,0" Orientation="Horizontal">
            <Button Width="110" Margin="0,0,8,0" Click="Start_Click" Content="Start control" />
            <Button Width="110" Click="Stop_Click" Content="Stop control" />
        </StackPanel>

        <Border Grid.Row="4" Margin="0,20,0,0" Padding="14" Background="#0D000000" CornerRadius="6">
            <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="Latest device truth" />
                <TextBlock x:Name="ActualText" Margin="0,10,0,0" Text="Actual: -- °C" />
                <TextBlock x:Name="TargetText" Margin="0,5,0,0" Text="Target: -- °C" />
                <TextBlock x:Name="RunningText" Margin="0,5,0,0" Text="Running: --" />
                <TextBlock x:Name="MessageText" Margin="0,12,0,0" TextWrapping="Wrap" Text="Connect the simulator to begin." />
            </StackPanel>
        </Border>
    </Grid>
</Window>
